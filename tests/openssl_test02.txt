# (Genera esto en tu carpeta ~/p1)
sudo openssl req -new -x509 \
  -newkey rsa:2048 \
  -keyout test02_sha1_key.pem \
  -out test02_sha1_cert.pem \
  -days 30 \
  -sha1 \
  -nodes \
  -subj "/CN=test.local" \
  -addext "subjectAltName=DNS:test.local,DNS:localhost"